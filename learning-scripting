* How to use your script as system command ?
# Move your script to the location where shell seaches for system commands($PATH).
----------------------------------------------------------------
echo $(ls)
#Prints the output of ls command.
--------------------------------------------------------------------
# When you are assigning values to variable space should not be given.
--------------------------------------------------------------------
<< hey ..............hey | Everything in between hey's is/are comments.
#Variable names should not start with numbers and any spaces.
  letters(a-z or A-Z),numbers (0-9) and underscore(_).
-----------------------------------------------------------------------
#setting date and time

 date -s "mention date and time here"
-------------------------------------------------------------------------
# Variables defined by you are called user defined variables, if variables defined by system then they are called system variables or environment variables.
To see system defined variables (type) #env
------------------------------------------------------------------------
# echo special characters : \n - creates new line from where it is used.
                            \b - used to remove one space back from where it is used.
                            \t - horizontal tab space.
                            \v - vertical tab space.
                            \a - create a sound alert.
                            \\ - escapes or eliminate the special purpose of your escaped characters.
-------------------------------------------------------------------------
# seq 1 10   (prints nums 1 to 10)
  seq 1 2 10 (prints nums 1 3 5 7 9;step = 2 )
# echo {1..10}  (prints nums 1 to 10)
  echo {1..10..2}(prints nums 1 3 5 7 9;step = 2 )
-----------------------------------------------------------------------------
# To open another trminal from the already opened terminal
 $gnome-terminal

# To open any browser from terminal

$firefox(any browser) http://www.your-link.com
---------------------------------------------------------------------------
# To create new users and groups
 $ useradd username
 $ passwd username
  # type password 2 times to fix.
 $ groupadd groupname
---------------------------------------------------------------------------
# To give permissions to users, edit the "visudo" file

